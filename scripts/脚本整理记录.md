# 脚本整理记录

**整理日期**: 2026-01-04  
**整理人**: Kiro AI Assistant

---

## 整理目标

清理根目录下的冗余脚本，建立清晰的脚本管理结构。

---

## 整理前状态

根目录有13个脚本文件：

### Windows批处理脚本（.bat）
1. check-hot-reload-status.bat
2. check-server-code.bat
3. deploy-with-mount.bat
4. deploy.bat ✅ 保留
5. push-and-rebuild.bat
6. restart-backend.bat
7. start.bat ✅ 保留

### Shell脚本（.sh）
1. check-server-code.sh
2. deploy-cloud.sh
3. deploy-server.sh
4. deploy.sh ✅ 保留
5. rebuild-frontend.sh
6. server-commands.sh

**问题**：
- 脚本分散在根目录，难以管理
- 缺少分类和说明
- 有些脚本已过期但仍保留
- 调试脚本和部署脚本混在一起

---

## 整理方案

### 创建目录结构
```
scripts/
├── README.md              # 脚本说明文档
├── 脚本整理记录.md         # 本文档
├── deployment/            # 部署脚本
├── debug/                 # 调试脚本
└── archive/               # 归档脚本
```

### 脚本分类

#### 保留在根目录（3个）
- **start.bat** - 本地启动脚本（Windows开发环境）
- **deploy.bat** - 本地部署脚本（Windows）
- **deploy.sh** - 本地部署脚本（Linux/Mac）

**原因**：这些是最常用的开发和部署脚本，保留在根目录方便快速访问。

#### 移动到 scripts/deployment/（2个）
- **deploy-cloud.sh** - 云服务器部署
- **deploy-server.sh** - 服务器部署

**原因**：生产环境部署脚本，使用频率较低，归类管理。

#### 移动到 scripts/debug/（4个）
- **check-server-code.bat** - 检查服务器代码版本
- **check-server-code.sh** - 检查服务器代码版本
- **check-hot-reload-status.bat** - 检查热更新状态
- **server-commands.sh** - 服务器调试命令

**原因**：这些是临时调试和问题排查脚本，不常用。

#### 移动到 scripts/archive/（4个）
- **deploy-with-mount.bat** - 带挂载的部署（已过期）
- **push-and-rebuild.bat** - 推送并重建（已过期）
- **restart-backend.bat** - 重启后端（已不推荐）
- **rebuild-frontend.sh** - 重建前端（已整合）

**原因**：这些脚本已过期或有更好的替代方案，归档保留作为历史参考。

---

## 整理操作

### 1. 创建目录结构
```bash
mkdir -p scripts/deployment
mkdir -p scripts/debug
mkdir -p scripts/archive
```

### 2. 移动脚本
```bash
# 部署脚本
git mv deploy-cloud.sh scripts/deployment/
git mv deploy-server.sh scripts/deployment/

# 调试脚本
git mv check-server-code.bat scripts/debug/
git mv check-server-code.sh scripts/debug/
git mv check-hot-reload-status.bat scripts/debug/
git mv server-commands.sh scripts/debug/

# 归档脚本
git mv deploy-with-mount.bat scripts/archive/
git mv push-and-rebuild.bat scripts/archive/
git mv restart-backend.bat scripts/archive/
git mv rebuild-frontend.sh scripts/archive/
```

### 3. 创建文档
- scripts/README.md - 脚本使用说明
- scripts/脚本整理记录.md - 本文档

---

## 整理后状态

### 根目录（3个核心脚本）
```
├── start.bat          # Windows本地启动
├── deploy.bat         # Windows本地部署
└── deploy.sh          # Linux/Mac本地部署
```

### scripts/目录（10个脚本 + 2个文档）
```
scripts/
├── README.md                           # 📖 脚本说明
├── 脚本整理记录.md                      # 📝 整理记录
├── deployment/                         # 🚀 部署脚本（2个）
│   ├── deploy-cloud.sh
│   └── deploy-server.sh
├── debug/                              # 🔧 调试脚本（4个）
│   ├── check-server-code.bat
│   ├── check-server-code.sh
│   ├── check-hot-reload-status.bat
│   └── server-commands.sh
└── archive/                            # 📦 归档脚本（4个）
    ├── deploy-with-mount.bat
    ├── push-and-rebuild.bat
    ├── restart-backend.bat
    └── rebuild-frontend.sh
```

---

## 整理效果

### 整理前
- ❌ 根目录有13个脚本文件
- ❌ 脚本用途不清晰
- ❌ 调试脚本和部署脚本混在一起
- ❌ 过期脚本仍在根目录

### 整理后
- ✅ 根目录只保留3个核心脚本
- ✅ 脚本按用途分类管理
- ✅ 有完整的使用说明文档
- ✅ 过期脚本已归档

---

## 使用建议

### 日常开发
```bash
# Windows
start.bat

# Linux/Mac
./deploy.sh
```

### 云服务器部署
```bash
./scripts/deployment/deploy-cloud.sh
```

### 问题排查
```bash
# 检查服务器代码
./scripts/debug/check-server-code.sh

# 检查容器状态
./scripts/debug/check-hot-reload-status.bat
```

### 查找脚本
1. 查看 `scripts/README.md` 了解所有可用脚本
2. 根据用途选择对应目录
3. 参考文档中的使用示例

---

## 后续维护

### 添加新脚本
1. 确定脚本用途（部署/调试/其他）
2. 放入对应目录
3. 更新 `scripts/README.md`
4. 添加使用说明和示例

### 归档旧脚本
1. 将不再使用的脚本移到 `scripts/archive/`
2. 在 `scripts/README.md` 中标注为"已过期"
3. 说明替代方案或原因

### 文档更新
- 每次添加/移动脚本后更新 `scripts/README.md`
- 保持文档和实际脚本同步
- 及时更新使用示例

---

## 总结

本次整理完成了脚本的分类管理：

✅ 根目录从13个脚本减少到3个核心脚本  
✅ 创建了清晰的目录结构（deployment/debug/archive）  
✅ 编写了完整的使用说明文档  
✅ 归档了4个过期脚本  
✅ 分类了6个专用脚本（2个部署 + 4个调试）  

项目根目录现在更加清爽，脚本管理更加规范！

---

**整理完成时间**: 2026-01-04  
**文档版本**: 1.0
